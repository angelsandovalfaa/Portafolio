{
  "name": "portafolio",
  "version": "1.0.0",
  "description": "This is a static portfolio website inspired by https://serhii-dev.webflow.io/",
  "main": "script.js",
  "scripts": {
    "start": "live-server",
    "dev": "npm run dev --prefix web",
    "build:web": "npm run build --prefix web",
    "preview:web": "npm run preview --prefix web",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
